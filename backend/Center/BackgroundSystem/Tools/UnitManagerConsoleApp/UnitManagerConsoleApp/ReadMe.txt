- the application UnitManagerConsoleApp.exe is used to make some integration tests for the UnitManager

Requirements:

- The comms should be launch to test UnitManager.

- The iqube list should be set into the UnitManagerConfig.xml file.
- It's better to test whether the iqubes defined in the config file are started.